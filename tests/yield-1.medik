machine Yielder {
  init state Loop {
    entry {
      print("first\n");
      yield;
      sleep(2);
      print("third");
    }
  }
}

init machine Tester {
  init state Test {
    entry {
      new Yielder();
      print("second\n");
      sleep(2);
    }
  }
}
